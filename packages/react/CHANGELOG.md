# Changelog

All notable changes to `@sepbase/react` are documented here.

## Unreleased

- Added opt-in `SepbaseV3Provider`, `useSepbaseV3Identity`, and
  `SepbaseV3Identity` bindings for same-block, forward-confirmed Unicode V3
  identity display without changing the legacy provider surface.
- Commit/reveal, text-record, offer and auction mutation hooks remain pending
  the V3 browser flow and candidate deployment.

## 0.1.0 — 2026-07-12

- Forward-confirmed identity provider, hooks and components for v2 deployments.
